package com.ahd.application;

//Interfaces that define the operations the application core expects from external systems (driven ports) and the operations the core provides to the outside world (driving ports).
//Driving ports could be use case interfaces, while driven ports might be repository interfaces or service interfaces for external systems.